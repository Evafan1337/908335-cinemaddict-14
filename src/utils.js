import {RenderPosition, render, renderTemplate} from './utils/renderUtils';
export {RenderPosition, render, renderTemplate};

import {compareValues, filmsInfoSort, getFilmsInfoSortLength, sortFilmsRated, sortFilmsCommented} from './utils/sortUtils';
export {compareValues, filmsInfoSort, getFilmsInfoSortLength, sortFilmsRated, sortFilmsCommented};

import {createElement, updateItem, replace, remove} from './utils/elementActionsUtils';
export {createElement, updateItem, replace, remove};